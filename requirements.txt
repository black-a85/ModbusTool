PySide6>=6.6
pymodbus>=3.6
pyserial>=3.5
psutil>=5.9

# Register Map (AI) feature
pdfplumber>=0.11      # PDF text extraction (permissive license)
pypdfium2>=4          # PDF page rendering for vision fallback (permissive)
pypdf>=5              # page-range PDF splitting (permissive)
anthropic>=0.60       # Claude cloud provider
openai>=1.40          # LM Studio / Ollama (OpenAI-compatible) provider
reportlab>=4          # test-only: generate sample register-map PDFs
